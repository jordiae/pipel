name: Build documentation

on:
  push

jobs:
  create_html:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build Sphinx docs
      run: |
        cd ./docs
        make html
